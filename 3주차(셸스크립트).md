# 3주차: 셸 스크립트 및 Ollama 서비스 구축 정리

## 1. 오늘 배운 핵심 내용
- **셸 스크립트(Shell Script)** 작성 및 실행 권한 설정
- **환경변수 설정**과 프로세스 실행 (`export`, `exec`)
- **Qwen & Ollama**: 로컬 LLM 서버 연동 구조 이해
- **셸 특수 변수 및 조건문**: `$0`, `$@`, `$?`, 리다이렉션(`>/dev/null 2>&1`)

---

## 2. 자주 발생하는 오류 및 해결

| 오류 메시지 | 주요 원인 | 해결 방법 |
| :--- | :--- | :--- |
| `Permission denied` | 스크립트 파일에 실행(`x`) 권한 없음 | `chmod u+x <파일명>` 실행 |
| `Command not found` | 현재 디렉터리가 `PATH`에 없거나 실행 경로 미지정 | `./<파일명>` 형식으로 실행 경로 명시 |

---

## 3. LLM & Ollama 서비스 아키텍처

### 3.1 Qwen & Ollama 개요
- **Qwen**: 알리바바(Alibaba)에서 개발한 오픈소스 대형 언어 모델(LLM).
- **Ollama**: 로컬 환경에서 LLM을 손쉽게 구동할 수 있도록 돕는 오픈소스 프레임워크/서버.

### 3.2 서비스 연동 구조
```text
[ 브라우저 ]
    │  질문 전송 / 답변 표시 (HTTP)
    ▼
[ Python 웹 앱 (chat.py) ]
    │  API 요청 전달 (JSON / REST API)
    ▼
[ Ollama 서버 (127.0.0.1:11434) ]
    │  모델 인퍼런스(추론) 실행
    ▼
[ LLM 모델 (Qwen3:0.6B) ]
```

### 3.3 모델 파라미터(B)의 의미
- **B (Billion)**: 10억 단위를 의미하며, 모델의 가중치(Parameter) 수를 나타냄.
  - 예: `0.6B` = 약 6억 개의 파라미터
- **파라미터 크기에 따른 특징**:
  1. **성능 향상**: 더 복잡한 언어 패턴, 문맥, 문법 규칙을 정교하게 학습/유지 가능.
  2. **자원 소모**: 연산량 증가로 인해 응답 속도가 느려지며, 더 많은 메모리(RAM/VRAM)와 저장 공간 요구.

---

## 4. 셸 스크립트 핵심 문법 분석

### 4.1 `start.sh` 스크립트 분석
```bash
#!/bin/bash
cd "$(dirname "$0")" || exit 1
exec python3 chat.py
```

- **`#!/bin/bash` (Shebang, 셔뱅)**:
  - 파일 첫 줄에 위치하여 해당 스크립트를 어떤 인터프리터로 해석할지 OS에 전달.
  - `#!/bin/bash`는 "이 파일은 Bash 셸로 실행하라"는 의미.
- **`$0` 및 `"$0"`**:
  - `$0`: 현재 실행된 스크립트의 경로/이름.
  - 따옴표(`"..."`)를 사용하는 이유: 디렉터리나 파일명에 공백이 포함되어 있을 때 경로가 깨지는 현상 방지.
- **`dirname "$0"`**:
  - 주어진 경로에서 파일명을 제외한 순수 디렉터리 경로만 추출.
- **`$(...)`**:
  - 명령어 치환(Command Substitution). 괄호 안 명령어의 실행 결과를 반환.
- **`|| exit 1`**:
  - 리눅스 명령어는 성공 시 `0`, 실패 시 `1` 이상의 종료 상태 코드(Exit Code)를 반환.
  - `||` (논리 OR): 앞선 명령(`cd`)이 실패하면 뒤의 `exit 1`을 실행하여 스크립트 즉시 중단.
- **`exec python3 chat.py`**:
  - 현재 실행 중인 Bash 프로세스를 새 프로세스(`python3`)로 완전히 대체. (불필요한 메모리 낭비 제거 및 시그널 직접 전달)

---

### 4.2 인자(Arguments) 및 특수 변수
```bash
$ ./command.sh qwen3:0.6b 8000
#    $0            $1       $2
```

- `$0`: 스크립트 파일명/경로
- `$1`, `$2`, ...: 전달된 n번째 위치 인자
- `$#`: 전달받은 총 인자의 개수
- `$@`: 전달받은 모든 인자 목록 (배열 형태)
- `$?`: 직전에 실행된 명령어의 종료 상태 코드 (`0`: 성공, 그 외: 실패)

---

### 4.3 `Heredoc`을 이용한 파일 생성 및 환경변수 주입
```bash
$ cat > start_with_export.sh << 'EOF'
#!/bin/bash
cd "$(dirname "$0")" || exit 1
export MODEL="qwen3:0.6b"
exec ./start.sh
EOF

$ chmod u+x start_with_export.sh
```

- **`cat > 파일명 << 'EOF'` (Heredoc)**:
  - 이후 입력되는 문자열을 파일에 직접 작성하며, 마지막 줄에 `EOF`가 입력되면 작성을 종료.
- **`export MODEL="..."`**:
  - 환경변수를 선언하여 현재 셸 및 하위 자식 프로세스(`start.sh`, `chat.py`)에서도 참조할 수 있도록 전달.

---

### 4.4 조건문과 입출력 리다이렉션
```bash
if ! command -v python3 >/dev/null 2>&1; then
    echo "Python3를 먼저 설치하세요." >&2
    exit 1
fi
```

- **기본 문법 구조**:
  ```bash
  if [명령어]; then
      # 참(종료 상태 0)일 때 실행
  fi
  ```
- **`command -v python3`**:
  - `python3` 명령어가 실행 가능한지 확인하고 경로를 출력. 없으면 실패 반환.
  - 앞에 붙은 `!`는 결과를 부정(NOT)함.
- **표준 스트림 번호 (File Descriptors)**:
  - `0`: 표준 입력 (`stdin`)
  - `1`: 표준 출력 (`stdout`)
  - `2`: 표준 오류 (`stderr`)
- **`>/dev/null 2>&1`**:
  - `>/dev/null`: 표준 출력(`1`)을 버림(휴지통 처리).
  - `2>&1`: 표준 오류(`2`)를 표준 출력(`1`)과 같은 곳으로 보냄.
  - **결과**: 화면에 어떤 출력이나 에러도 띄우지 않고 오직 명령어의 성공/실패 여부만 확인.
- **`echo "..." >&2`**:
  - 안내 메시지를 일반 출력이 아닌 표준 오류(`stderr`) 스트림으로 전달.